import React from "react";
import classes from "./Project.module.css";

const Project = function (title) {
    console.log(classes.mainBlock)
    return(
        <div className={classes.mainBlock}>
            
        </div>
    )
}
export default Project;